export const NOTIFICATION_DEFAULTS = {
  /** Maximum amount of notifications shown on screen at the same time. */
  MAX_NOTIFICATION_SHOWN: 5,

  /** Notification duration on screen. */
  DURATION: 5000,
};
